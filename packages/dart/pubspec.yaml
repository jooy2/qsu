name: qsu
description: >-
  Everyday helpers you would otherwise write yourself: slugs, text case, date math, file size formatting, deep clone and merge, validation and hashing.
version: 1.7.0
repository: https://github.com/jooy2/qsu
homepage: https://qsu.cdget.com

topics:
  - utility
  - helper
  - string
  - validation
  - crypto

environment:
  sdk: ^3.5.0

dependencies:
  path: ^1.9.1
  crypto: ^3.0.7
  unorm_dart: ^0.3.2
  # 2.2.0 raised its own floor to Dart 3.7, and this package supports 3.5.
  ffi: ^2.1.3

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.15
