import{_ as o,C as s,o as r,c as n,ah as a,E as i}from"./chunks/framework.C2WMkNym.js";const g=JSON.parse('{"title":"qsu next to the other utility libraries","description":"qsu next to Lodash, Underscore.js, quiver, basic_utils, pydash and boltons, area by area.","frontmatter":{"order":1,"menuTitle":"Comparison","description":"qsu next to Lodash, Underscore.js, quiver, basic_utils, pydash and boltons, area by area."},"headers":[],"relativePath":"discover-more/comparison.md","filePath":"en/discover-more/comparison.md","lastUpdated":1789967602000}'),d={name:"discover-more/comparison.md"};function c(h,e,l,p,u,m){const t=s("FeatureMatrix");return r(),n("div",null,[e[0]||(e[0]=a('<h1 id="qsu-next-to-the-other-utility-libraries" tabindex="-1">qsu next to the other utility libraries <a class="header-anchor" href="#qsu-next-to-the-other-utility-libraries" aria-label="Permalink to &quot;qsu next to the other utility libraries&quot;">​</a></h1><p>qsu is not the first utility library any of its three languages has had, and for most projects it will not be the only one. This page says what it covers, what the better-known alternatives cover, and which of them is the right answer for a given job.</p><p>Pick a language in the sidebar and the comparison follows it. Each library also has a <a href="/discover-more/">migration guide</a> of its own.</p><h2 id="the-libraries-compared" tabindex="-1">The libraries compared <a class="header-anchor" href="#the-libraries-compared" aria-label="Permalink to &quot;The libraries compared&quot;">​</a></h2><div class="lang-only" data-code-lang="js"><p><strong><a href="https://lodash.com" target="_blank" rel="noreferrer">Lodash</a></strong> is the one almost every JavaScript project has met. It is a large, stable collection built around arrays, objects and strings, with a functional layer on top: currying, chaining, iteratee shorthands and a <code>fp</code> build.</p><p><strong><a href="https://underscorejs.org" target="_blank" rel="noreferrer">Underscore.js</a></strong> came first and is smaller. Lodash started as a fork of it, so the two share most of their names, and Underscore has since kept to a narrower set rather than growing to match.</p></div><div class="lang-only" data-code-lang="dart"><p><strong><a href="https://pub.dev/packages/quiver" target="_blank" rel="noreferrer">quiver</a></strong> is published by Google under Apache-2.0. It reads as a set of additions to the SDK rather than a utility belt: iterables in the shape of Python&#39;s <code>itertools</code>, collection types the SDK lacks such as <code>BiMap</code> and <code>Multimap</code>, caches, argument checks and an injectable <code>Clock</code>.</p><p><strong><a href="https://pub.dev/packages/basic_utils" target="_blank" rel="noreferrer">basic_utils</a></strong> is a collection of <code>*Utils</code> classes with static methods: <code>StringUtils</code>, <code>MathUtils</code>, <code>IterableUtils</code>, <code>EmailUtils</code>, <code>DomainUtils</code>, <code>HttpUtils</code>, <code>DnsUtils</code> and a large certificate and cryptography section built on <code>X509Utils</code>, <code>PKCS12Utils</code> and <code>ASN1Utils</code>.</p></div><div class="lang-only" data-code-lang="python"><p><strong><a href="https://pydash.readthedocs.io" target="_blank" rel="noreferrer">pydash</a></strong> is a port of Lodash. The names are the same in <code>snake_case</code>, the arguments follow Lodash&#39;s, and the chaining and callback shorthands come with them.</p><p><strong><a href="https://boltons.readthedocs.io" target="_blank" rel="noreferrer">boltons</a></strong> is a set of pure-Python modules &quot;in the same spirit as the standard library&quot;: <code>strutils</code>, <code>iterutils</code>, <code>fileutils</code>, <code>timeutils</code>, <code>cacheutils</code> and about twenty more. It reads as the parts of the standard library that were never written, so it follows <code>stdlib</code> naming rather than any library&#39;s own.</p></div><h2 id="feature-sheet" tabindex="-1">Feature sheet <a class="header-anchor" href="#feature-sheet" aria-label="Permalink to &quot;Feature sheet&quot;">​</a></h2><p>Every row is one question, asked of qsu and of the two libraries beside it. The sheet follows the language switch, so the columns are the ones your ecosystem has, and a question only one ecosystem can answer — certificates in Dart, atomic writes in Python — appears only on that sheet.</p><p>Read a mark as a starting point rather than a verdict. Two functions can both earn a check and still differ in their arguments, their defaults or their edge cases, which is what the migration guides are for.</p>',10)),i(t),e[1]||(e[1]=a('<h2 id="what-qsu-does-not-do" tabindex="-1">What qsu does not do <a class="header-anchor" href="#what-qsu-does-not-do" aria-label="Permalink to &quot;What qsu does not do&quot;">​</a></h2><p>The table above is drawn around qsu, so here is the other direction.</p><p>qsu has no chaining, no currying and no callback shorthand. Every function is called on its own and given real arguments, which is why there is no <code>_.chain</code>, no <code>fp</code> build and no iteratee written as a string or an object.</p><p>It has no higher-order function toolkit either: nothing for <code>memoize</code>, <code>once</code>, <code>curry</code>, <code>partial</code> or function composition. <code>debounce</code>, <code>throttle</code> and <code>retry</code> are there because they solve a timing problem, not because qsu wraps functions in general.</p><p>It leaves to the language whatever the language already does well. Mapping, filtering, reducing, sorting, slicing, grouping and joining are all one call in modern JavaScript, Dart and Python, so qsu does not reimplement them.</p><div class="lang-only" data-code-lang="js"><p>Lodash also carries pieces qsu has no counterpart for at any level: <code>_.template</code>, the sorted-index family, <code>_.zip</code> and <code>_.unzip</code>, <code>_.orderBy</code> with several keys and directions, and the mutating helpers <code>_.pull</code>, <code>_.remove</code> and <code>_.fill</code>.</p></div><div class="lang-only" data-code-lang="dart"><p>quiver&#39;s collection types have no counterpart in qsu. If you need a <code>BiMap</code>, a <code>Multimap</code>, an <code>LruMap</code> or a <code>TreeSet</code>, that is what quiver is for, and nothing in qsu replaces it. The same goes for its injectable <code>Clock</code>, which exists so that time-dependent code can be tested.</p><p>basic_utils reaches much further into certificates than qsu does. qsu hashes, encodes and encrypts; it does not read or write X.509, PKCS12 or ASN.1.</p></div><div class="lang-only" data-code-lang="python"><p>pydash carries the whole functional layer that qsu leaves out, and much of its collection API returns to what <code>map</code>, <code>filter</code> and <code>itertools</code> already give you. boltons goes deeper than qsu on nested data with <code>remap</code> and <code>research</code>, on atomic file writes with <code>atomic_save</code>, and on data structures such as <code>OrderedMultiDict</code> and <code>IndexedSet</code>.</p></div><h2 id="how-the-api-is-shaped" tabindex="-1">How the API is shaped <a class="header-anchor" href="#how-the-api-is-shaped" aria-label="Permalink to &quot;How the API is shaped&quot;">​</a></h2><p>Every qsu function is a plain function with a <code>camelCase</code> name. Optional arguments arrive as one options value at the end, written as an object in JavaScript, as named parameters in Dart and as keyword arguments or a single <code>dict</code> in Python. There is one name per behavior rather than a family: <a href="/reference/string/pad">pad</a> takes a <code>position</code> option instead of splitting into three functions, and <a href="/reference/object/objClone">objClone</a> takes <code>deep</code> instead of being two.</p><div class="lang-only" data-code-lang="js"><p>Lodash and Underscore both put the data first and return a new value, which is the same shape as a qsu call. What differs is the layer above it. Lodash accepts a property name, an object or an array where a callback is expected, and <code>_.get</code> and <code>_.set</code> read a path written as a string. qsu takes callbacks as callbacks; only <a href="/reference/object/objGet">objGet</a> reads a path.</p></div><div class="lang-only" data-code-lang="dart"><p>quiver and basic_utils are shaped differently from each other. quiver exports top-level functions, so <code>isBlank(s)</code> reads the way <code>trim(s)</code> does in qsu. basic_utils groups everything into classes of static methods, so the same call is <code>StringUtils.isNullOrEmpty(s)</code>. qsu follows quiver here: the functions are top-level and imported from one place.</p><p>The name is where qsu is deliberately unidiomatic. <code>strToCamelCase</code> rather than <code>toCamelCase</code> or an extension method, because the Dart package must read the same as the JavaScript and Python ones.</p></div><div class="lang-only" data-code-lang="python"><p>pydash suffixes the names that collide with builtins, so you write <code>pydash.map_</code>, <code>pydash.filter_</code> and <code>pydash.sum_</code>. boltons follows the standard library and puts each group in its own module, so you import <code>from boltons.strutils import slugify</code>.</p><p>qsu does neither. The names are <code>camelCase</code> in Python too, and they are imported from the package root. That is not idiomatic Python, and it is the price of the API being identical in three languages.</p></div><h2 id="where-the-code-runs" tabindex="-1">Where the code runs <a class="header-anchor" href="#where-the-code-runs" aria-label="Permalink to &quot;Where the code runs&quot;">​</a></h2><div class="lang-only" data-code-lang="js"><p>qsu is ESM only and needs Node 20 or later. It has no dependencies, it is marked side-effect free, and each category has a subpath of its own, so <code>import { trim } from &#39;qsu/string&#39;</code> pulls in that category alone. Anything that needs a Node runtime lives behind <code>qsu/node</code>, which keeps the browser-safe half browser-safe. The TypeScript types are part of the package.</p><p>Lodash&#39;s published entry point is a single CommonJS file with no <code>exports</code> map; the ESM build is a separate package, <code>lodash-es</code>, and tree-shaking a bundle depends on using it. Underscore ships both builds from one package through conditional exports. Neither ships its own type declarations, so both are typed from DefinitelyTyped.</p></div><div class="lang-only" data-code-lang="dart"><p>qsu needs Dart 3.5 or later and depends on <code>path</code>, <code>crypto</code>, <code>unorm_dart</code>, <code>ffi</code> and <code>pointycastle</code>. The <code>os</code> category reaches the operating system through <code>dart:ffi</code> on macOS and Windows and through <code>/proc</code> on Linux and Android, so it throws an <code>UnsupportedError</code> on the web while the rest of the package keeps working there.</p><p>quiver is pure Dart and carries one dependency. basic_utils is pure Dart as well but pulls in several of its own, including an HTTP client and a cryptography implementation, which is what the certificate half of the package is built on.</p></div><div class="lang-only" data-code-lang="python"><p>qsu needs Python 3.8 or later and depends on <code>cryptography</code>, which is what the <code>crypto</code> category is built on.</p><p>pydash is pure Python and needs 3.10 or later, so it is the one to check first if you are held on an older interpreter. boltons is pure Python with no dependencies at all and supports a wider range of versions than either.</p></div><h2 id="the-part-that-is-only-qsu" tabindex="-1">The part that is only qsu <a class="header-anchor" href="#the-part-that-is-only-qsu" aria-label="Permalink to &quot;The part that is only qsu&quot;">​</a></h2><p>The same function, under the same name, with the same result, in JavaScript, Dart and Python. A service whose API is written in Node, whose app is written in Flutter and whose data jobs are written in Python can slugify a title, format a file size, validate an email address and hash a value the same way in all three, and a developer moving between them keeps their utility belt.</p><p>None of the libraries on this page tries to do that, because none of them has anywhere to do it. Lodash is JavaScript, quiver is Dart, boltons is Python. pydash comes closest by porting Lodash&#39;s names, but a port is a resemblance rather than a guarantee: the two projects are maintained separately and neither is answerable to the other&#39;s behavior.</p><p>Parity is not a slogan here. It is enforced by the tests, which are ported alongside the implementation, and the few functions that exist in only one package are <a href="/reference/">marked as such</a> rather than quietly missing.</p><h2 id="which-to-use" tabindex="-1">Which to use <a class="header-anchor" href="#which-to-use" aria-label="Permalink to &quot;Which to use&quot;">​</a></h2><p>Use Lodash, quiver, basic_utils, pydash or boltons for what they are good at. They do not conflict with qsu, and there is no reason to remove one just to add the other.</p><p>Reach for qsu when you want one API across languages, when you need what it covers and the other library does not (file sizes, durations, slugs, hashing, machine information, file handling), or when you would otherwise write the helper yourself for the fourth time.</p><p>Stay where you are when you depend on a functional layer qsu does not have, when your code is built on a collection type it does not provide, or when the helpers you use are already in your language&#39;s standard library.</p>',25))])}const f=o(d,[["render",c]]);export{g as __pageData,f as default};
