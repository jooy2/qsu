# logBox <Lang js python />

<NodeRequired ko />

전달한 값들을 테두리가 있는 표 형태로 콘솔에 출력합니다. 인자 하나가 한 행이 되며, 길거나 구조가 있는 값도 읽기 쉽게 표시됩니다. 박스 너비는 터미널 크기에 맞춰지고, 객체는 `[object Object]` 대신 내용이 펼쳐집니다.

터미널 너비를 넘는 값은 자동으로 줄바꿈되며, 한글·한자·이모지처럼 폭이 넓은 문자는 표시 너비를 기준으로 계산하므로 테두리가 어긋나지 않습니다.

## Parameters

<ParamsTable :rows="[
	{ name: 'args', type: '...any', desc: '출력할 값들입니다. 각 값이 한 행이 됩니다. 인자를 주지 않으면 빈 표가 출력됩니다.' }
]" />

## Returns

> void

## Examples

::: lang js

```javascript
import { logBox } from 'qsu/node';

logBox([1, 2, 3, 4, 5]);
logBox('hello', { a: 1 });
```

:::

::: lang python

```python
logBox([1, 2, 3, 4, 5])
logBox('hello', {'a': 1})
```

:::

출력 예시:

```text
┌───┬──────────────────────────────────────────────────────────┐
│ # │ value                                                    │
├───┼──────────────────────────────────────────────────────────┤
│ 0 │ [ 1, 2, 3, 4, 5 ]                                        │
└───┴──────────────────────────────────────────────────────────┘
```
